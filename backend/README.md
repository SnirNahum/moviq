## Quick Start

### 1. Create `.env` file
Copy `.env.example` to `.env` and adjust values if needed.

### 2. Prepare the project
This will install dependencies, start the database, and run migrations.

```bash
npm run bootstrap
```

### 3. Start the development server

```bash
npm run dev
```